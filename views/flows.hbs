<!--SDPX-License-Identifier: MIT-->
<!--SDPX-FileCopyrightText: 2022 Sarah Julia Kriesch <sarah.j.kriesch@fau.de>-->
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Authentication-Flows</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
      img {
        max-width: 10%;
        height: auto;
        object-fit: cover;
      }
    </style>
</head>
<body>
    <div class="container">
        <center>
            <img class="mx-auto d-block mt-2" src="/images/logo-amos.png" alt="Image"  max-width="200" height="180">
        </center>
        <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
            <a href="/api/discovery/issuer/find?issuer_s={{issuer_s}}" role="button" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
                <span class="btn btn-outline-secondary me-2">Back to Discovery</span>
            </a>

            <a href="/api/protocol/logger" class="nav col-12 col-md-auto mb-2 mb-md-0 text-decoration-none">
                <span class="btn btn-outline-secondary me-2">Protocol</span>
            </a>

            <a class="col-md-3 text-end">
                <span class="btn me-2"></span>
            </a>
        </header>
    </div>

    <div class="container">
        <h2>Authentication-Flows</h2>
        <p>Please select the authentication-flow you want to test:</p>

        <div class="card-deck">        
            {{#if allowClientCredentials}}
            <div class="card text-center border-success m-3">
                <div class="card-header">
                    Client-Credentials
                </div>
                <div class="card-body">
                    <p class="card-text">Normally used for maschine-2-maschine-communication.</p>
                    <a href="/api/flows/cc?issuer_s={{issuer_s}}" class="btn btn-outline-dark">Try it out</a>
                </div>
            </div>
            {{else}}
            <div class="card text-center border-danger m-3">
                <div class="card-header">
                    Client-Credentials
                </div>
                <div class="card-body">
                    <p class="card-text">Normally used for maschine-2-maschine-communication.</p>
                    <a href="/api/flows/cc?issuer_s={{issuer_s}}" class="btn btn-outline-dark disabled">Try it out</a>
                </div>
            </div>
            {{/if}}
            {{#if allowPasswordGrant}}
            <div class="card text-center border-success m-3">
                <div class="card-header">
                    Password-Grant
                </div>
                <div class="card-body">
                    <p class="card-text">Combines the client-credentials-flow with a user-authentication.</p>
                    <a href="/api/flows/pg?issuer_s={{issuer_s}}" class="btn btn-outline-dark">Try it out</a>
                </div>
            </div>
            {{else}}
            <div class="card text-center border-danger m-3">
                <div class="card-header">
                    Password-Grant
                </div>
                <div class="card-body">
                    <p class="card-text">Combines the client-credentials-flow with a user-authentication.</p>
                    <a href="/api/flows/pg?issuer_s={{issuer_s}}" class="btn btn-outline-dark disabled">Try it out</a>
                </div>
            </div>
            {{/if}}
            {{#if allowAuthorizationCode}}
            <div class="card text-center border-success m-3">
                <div class="card-header">
                    Authorization-Code-Flow
                </div>
                <div class="card-body">
                    <p class="card-text">Redirects the user to the identity-provider to handle authentication.</p>
                    <a href="/api/flows/auth?issuer_s={{issuer_s}}" class="btn btn-outline-dark">Try it out</a>
                </div>
            </div>
            {{else}}
            <div class="card text-center border-danger m-3">
                <div class="card-header">
                    Authorization-Code-Flow
                </div>
                <div class="card-body">
                    <p class="card-text">Redirects the user to the identity-provider to handle authentication.</p>
                    <a href="/api/flows/auth?issuer_s={{issuer_s}}" class="btn btn-outline-dark disabled">Try it out</a>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>