{
	"$id": "example.schema.json",
	"title": "Example",
	"description": "Exapmle schema with some parameters to be used as template",
	"type": "object",
	"properties": {
		"typ": {
			"description": "Type of the token. E.g. Bearer",
			"type": "string"
		},
		"clientId": {
			"description": "ClientID for which the token was issued",
			"type": "string"
		},
		"non-existing": {
			"description": "Non-existing property for testing purposes",
			"type": "string"
		}
	},
	"required": [ "clientId", "non-existing" ]
}
